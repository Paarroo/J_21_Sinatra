<html>
   <head>
     <title>Créer un potin</title>
     <style>
     body {
       text-align: center;
       font-family: Arial, sans-serif;
       padding: 20px;
     }

     h1 {
       text-align: center;
       color: #E7BD04FF;
       margin-top: 70px;
       font-size: 2.8em;
       margin-bottom: 30px;
       text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
     }

     p {
       color: #08BBDFFF;
       font-size: 1.2em;
       margin: 20px 0;              /* Corrigé */
       padding: 15px;
       background: rgba(255,255,255,0.8);
       border-radius: 10px;
       box-shadow: 0 2px 5px rgba(0,0,0,0.1);
     }

     a {
       color: #002FFFFF;
       text-decoration: none;
       font-size: 1em;
       display: block;              /* Pour que le lien prenne toute la zone */
       padding: 10px;
       border-radius: 5px;
       transition: all 0.3s ease;
     }

     a:hover {
       color: #1BAC04FF;
       background-color: rgba(0, 47, 255, 0.1);
       transform: scale(1.05);      /* Réduit l'effet (1.2 était trop) */
     }

     .link {
       display: inline-block;       /* Changé : pas besoin de flex pour un seul lien */
       font-size: 1.4em;           /* Plus gros pour le lien principal */
       margin-top: 40px;
       padding: 15px 30px;
       background: linear-gradient(135deg, #E7BD04FF 0%, #08BBDFFF 100%);
       color: white !important;     /* Force la couleur blanche */
       border-radius: 25px;
     }

     .link:hover {
       color: white !important;
       transform: scale(1.1);
       box-shadow: 0 5px 15px rgba(0,0,0,0.2);
     }

     hr {
       border: none;
       height: 1px;
       background: linear-gradient(135deg, #E7BD04FF 0%, #08BBDFFF 100%);
       margin: 20px 0;
     }
     </style>
   </head>
   <body>
     <h1>The famous potins</h1>

     <% gossips.each_with_index do |gossip, index| %>
       <p>
         <a href="/gossips/<%= index + 1 %>">
           <strong>Auteur :</strong> <%= gossip.author %><br/>
           <strong>Contenu :</strong> <%= gossip.content %>
         </a>
       </p>
       <hr>
     <% end %>

     <div style="margin-top: 40px;">
       <a href="/" class="link">
         🏠 Return to the main page
       </a>
     </div>
   </body>
</html>
